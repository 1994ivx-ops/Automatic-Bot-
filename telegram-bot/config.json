{
  "_comment": "DO NOT put real values here. Set all secrets as Railway environment variables.",
  "api_id": "${API_ID}",
  "api_hash": "${API_HASH}",
  "control_bot_token": "${CONTROL_BOT_TOKEN}",
  "your_personal_telegram_id": "${PERSONAL_TELEGRAM_ID}",
  "proxy_host": "${PROXY_HOST}",
  "proxy_port": "${PROXY_PORT}",
  "proxy_username": "${PROXY_USERNAME}",
  "proxy_password": "${PROXY_PASSWORD}"
}
